export const metadata = {
    title: "Delivery Partner Dashboard | Vendora",
    description: "Delivery Partner Dashboard of Vendora",
};

export default function DeliveryDashboardLayout({ children }) {
    return <>{children}</>;
}