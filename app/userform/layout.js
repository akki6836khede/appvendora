export const metadata = {
    title: "Customer Form | Vendora",
    description: "Customer Form of Vendora",
};

export default function CustomerFormLayout({ children }) {
    return <>{children}</>;
}